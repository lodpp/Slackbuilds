# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

             |-----handy-ruler------------------------------------------------------|
git-mediawiki: git-mediawiki
git-mediawiki:
git-mediawiki: Git-Mediawiki is a project which aims the creation of a gate between
git-mediawiki: git and mediawiki, allowing git users to push and pull objects from
git-mediawiki: mediawiki just as one would do with a classic git repository thanks
git-mediawiki: to remote-helpers.
git-mediawiki:
git-mediawiki:
git-mediawiki:
git-mediawiki:
git-mediawiki:
